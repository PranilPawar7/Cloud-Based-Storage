# Cloud-Based-Storage

### **📁 Project Structure:**
```
cloud-backup-system/
├── public/
│   └── index.html          (Use code from artifact)
├── src/
│   ├── App.js              (Use code from artifact)
│   ├── App.css             (Use code from artifact)
│   ├── firebase.js         (Use code from artifact)
│   ├── index.js            (Use code from artifact)
│   └── index.css           (Use code from artifact)
└── package.json            (Already exists, update if needed)
